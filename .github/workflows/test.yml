name: "\U0001F9EA Test & Coverage"

on:
  workflow_call:
    inputs:
      node-version:
        description: "Node.js version"
        type: string
        default: "20"
      pnpm-version:
        description: "pnpm version (leave empty to use the repo's package.json packageManager field)"
        type: string
        default: ""
      build-before-test:
        description: "Run pnpm build before tests"
        type: boolean
        default: false
      test-command:
        description: "Test command to run"
        type: string
        default: "pnpm test:coverage"
    secrets:
      NPM_TOKEN:
        required: false

jobs:
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    permissions:
      contents: read

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}

      # pnpm/action-setup hard-fails when `version` is passed AND the repo's
      # package.json pins `packageManager` ("Multiple versions of pnpm
      # specified"). Only pass `version` when the caller explicitly set one.
      - name: Install pnpm (version from package.json packageManager)
        if: ${{ inputs.pnpm-version == '' }}
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Install pnpm (version pinned by caller)
        if: ${{ inputs.pnpm-version != '' }}
        uses: pnpm/action-setup@v4
        with:
          version: ${{ inputs.pnpm-version }}
          run_install: false

      - name: Get pnpm store directory
        shell: bash
        run: echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: Setup pnpm cache
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: Configure npm registry
        # secrets.* is not a valid named-value in step `if` expressions of a
        # called workflow — guard in bash instead
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
        shell: bash
        run: |
          if [ -n "$NPM_TOKEN" ]; then
            echo "//registry.npmjs.org/:_authToken=${NPM_TOKEN}" > .npmrc
          fi

      - name: Install dependencies
        run: pnpm install

      - name: Build
        if: ${{ inputs.build-before-test }}
        run: pnpm build

      - name: Run tests with coverage
        run: ${{ inputs.test-command }}

      - name: Extract coverage percentage
        if: always()
        id: coverage
        run: |
          if [ -f coverage/coverage-summary.json ]; then
            TOTAL=$(node -e "const c = require('./coverage/coverage-summary.json'); console.log(Math.round(c.total.lines.pct))")
            echo "pct=$TOTAL" >> $GITHUB_OUTPUT
            if [ "$TOTAL" -ge 80 ]; then
              echo "color=brightgreen" >> $GITHUB_OUTPUT
            elif [ "$TOTAL" -ge 60 ]; then
              echo "color=green" >> $GITHUB_OUTPUT
            elif [ "$TOTAL" -ge 40 ]; then
              echo "color=yellow" >> $GITHUB_OUTPUT
            else
              echo "color=red" >> $GITHUB_OUTPUT
            fi
          else
            echo "pct=0" >> $GITHUB_OUTPUT
            echo "color=red" >> $GITHUB_OUTPUT
          fi

      - name: Coverage summary
        if: always()
        run: |
          echo "## Coverage Report" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "![Coverage](https://img.shields.io/badge/coverage-${{ steps.coverage.outputs.pct }}%25-${{ steps.coverage.outputs.color }})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          if [ -f coverage/coverage-summary.json ]; then
            node -e "
              const c = require('./coverage/coverage-summary.json').total;
              console.log('| Metric | Percentage |');
              console.log('|--------|-----------|');
              console.log('| Lines | ' + c.lines.pct + '% |');
              console.log('| Functions | ' + c.functions.pct + '% |');
              console.log('| Branches | ' + c.branches.pct + '% |');
              console.log('| Statements | ' + c.statements.pct + '% |');
            " >> $GITHUB_STEP_SUMMARY
          fi

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/
          retention-days: 14
